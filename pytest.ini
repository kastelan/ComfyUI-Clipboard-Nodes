[pytest]
# clipboard_backend.py and nodes.py live at the repo root (ComfyUI custom
# node layout), not in a package — add root to sys.path so `import nodes`
# and `import clipboard_backend` work from tests/.
pythonpath = .
testpaths = tests
